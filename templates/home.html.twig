{% extends 'layouts/base.html.twig' %}

{% block content %}
    {% for message in app.flashes('welcomeFlash') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}
    <h1>This is the posts site</h1>
{% endblock %}